'''using math module to find the power of a number'''
import math

base = int(input("Enter the base: "))
exponent = int(input("Enter the exponent: "))
result = math.pow(base, exponent)
print("Result:", result)